/** Inline button spinner shown while a mock async action is in flight. */
export function Spinner() {
  return <div className="w-5 h-5 border-2 border-white/30 border-t-white rounded-full animate-spin" />;
}
